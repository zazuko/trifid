.idea
coverage
node_modules
npm-debug.log
*.tgz
dist

# The root .gitignore ignores all *.d.ts files (generated declarations now live
# in dist/), but the hand-written ambient declarations under types/ are source
# files and must be tracked.
!types/*.d.ts
